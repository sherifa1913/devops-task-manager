apiVersion: v1
kind: Service
metadata:
  name: taskmanager-frontend
spec:
  selector:
    app: taskmanager-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007
  type: NodePort
