global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alert-rules.yaml

scrape_configs:
  - job_name: "taskmanager-backend"
    metrics_path: /metrics
    static_configs:
      - targets: ["taskmanager-backend:5000"]
